#include "TreeNode.h" // Include the header file of the class

TreeNode::TreeNode(WordPair *pair) {
    data = pair; // sets the data as pair
    left = nullptr; // sets the left as null
    right = nullptr; // sets the right as null
}